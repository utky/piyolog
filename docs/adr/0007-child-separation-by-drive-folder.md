# ADR-0007: 子供の区別は Drive フォルダで行う

## 状態
確定

## 決定
子供ごとに Drive サブフォルダを分け、child_name はフォルダ名から付与する（本文パースに依存しない）。

## 根拠
ファイル名に子供名が無く、同年月のファイルが子供間で衝突する。フォルダ分離が最も素直。洗い替えキーは `(child_name, source_year_month)`。
