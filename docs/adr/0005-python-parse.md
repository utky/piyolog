# ADR-0005: パース処理は BQ 完結を断念し Python で実施

## 状態
確定

## 決定
テキストの行単位パースを Python の手続き的処理で行う。

## 根拠
「次のタブ区切り行が来るまで前の詳細テキストに追記する」という状態を持った逐次処理が必要で、純粋な SQL では表現が難しい。JS UDF でも可能だが、状態を持つ処理は手続き型言語が自然。実装言語は BQ/dbt エコシステムとの親和性から Python。
